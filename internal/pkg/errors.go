package pkg

const (
	UnexpectedMethod = "unexpected signing method: %v"
	WrongAmount      = "invalid amount, must not be 0 or less"
	NotEnoughFunds   = "the current wallet has insufficient funds for transaction"
)
